How can you embed an image with alternative text in HTML?,
a. <img src="image.jpg" alt="Alternative Text"> T,
b. <image src="image.jpg" alt="Alternative Text"> F,
c. <img src="image.jpg" title="Alternative Text"> F,
d. <image src="image.jpg"> F,
What is the purpose of the HTML <figure> element?,
a. Defines a caption for a table F,
b. Represents any content that is referenced from the main content T,
c. Sets the figure layout for an image F,
d. Specifies a numerical figure F,
How can you embed a video with controls in HTML?,
a. <video src="video.mp4" controls> F,
b. <embed src="video.mp4" controls> F,
c. <video controls="true" src="video.mp4"> T,
d. <media src="video.mp4" controls> F,
What is the purpose of the HTML attribute "alt" in an image tag?,
a. Specifies the alignment of the image F,
b. Adds a title to the image F,
c. Provides alternative text for the image T,
d. Sets the image as an alternative to the text F,
How can you provide a text description for an input field in HTML for accessibility purposes?,
a. <input description="..."> F,
b. <input alt="..."> F,
c. <input aria-label="..."> T,
d. <input caption="..."> F,
What HTML tag is used to embed a sound file in a web page?,
a. <sound> F,
b. <audio> F,
c. <embed> F,
d. <audio> T,
How can you ensure that your HTML document is accessible to screen readers?,
a. Use semantic HTML elements T,
b. Add background music to the page F,
c. Set font size to a fixed value F,
d. Remove all images from the document F,
What is the purpose of the HTML attribute "role" in accessibility?,
a. Specifies the role of the document F,
b. Defines the layout of the page F,
c. Specifies the role of an element in relation to the accessibility API T,
d. Sets the role of a form F,
How can you create a hyperlink with an accessible description in HTML?,
a. <a href="url">Link</a> F,
b. <a href="url" title="Accessible Link">Link</a> F,
c. <a href="url" aria-label="Accessible Link">Link</a> T,
d. <a href="url" alt="Accessible Link">Link</a> F,
What is the purpose of the HTML <figcaption> element when used with the <figure> element?,
a. Defines a caption for a table F,
b. Represents a footer for a figure F,
c. Specifies a title for a figure T,
d. Adds a border around a figure F,
END
