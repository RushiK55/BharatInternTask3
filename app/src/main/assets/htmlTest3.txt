What is the purpose of the HTML5 <article> element?,
a. Represents a navigation menu F,
b. Defines a self-contained piece of content that could be distributed and reused independently T,
c. Specifies an article's publication date F,
d. Sets the layout for the webpage F,
How can you create a responsive design in HTML?,
a. Using the <responsive> tag F,
b. Applying media queries in CSS T,
c. Using the <flexbox> attribute F,
d. Setting the viewport size in HTML F,
What is the purpose of the HTML <progress> element?,
a. Represents a progress bar T,
b. Declares the progress of an image loading F,
c. Defines a mathematical progress F,
d. Specifies the progress of a form submission F,
How can you create a tooltip in HTML?,
a. Using the <tooltip> tag F,
b. Adding the title attribute to an element T,
c. Applying the hover class in CSS F,
d. Using the <hint> element F,
What is the purpose of the HTML <nav> element?,
a. Represents a section that is quoted from another source F,
b. Defines a navigation menu T,
c. Specifies a navigation link F,
d. Sets the background color of the navigation bar F,
How can you embed an iframe in HTML?,
a. <frame> F,
b. <iframe> T,
c. <embed> F,
d. <window> F,
What does the HTML <time> element represent?,
a. Clock time F,
b. Represents a specific period in time T,
c. Timer countdown F,
d. Timestamp F,
How can you create a drop-down list in HTML?,
a. <dropdown><option></option></dropdown> F,
b. <list><dropdown></dropdown></list> F,
c. <select><option></option></select> T,
d. <menu><item></item></menu> F,
What is the purpose of the HTML <main> element?,
a. Defines the main content of the page T,
b. Specifies the main heading of the page F,
c. Represents the main section of the website F,
d. Declares the primary navigation of the page F,
How can you create a hyperlink that opens in a new tab or window?,
a. <a href="url" target="_self"> F,
b. <a href="url" target="_parent"> F,
c. <a href="url" target="_blank"> T,
d. <a href="url" target="_top"> F,
END
